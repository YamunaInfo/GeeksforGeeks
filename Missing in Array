class Solution {
    int missingNum(int arr[]) {
        long n=arr.length;
        long esum=(n+1)*(n+2)/2;
        long  asum=0;
        for(int i=0;i<arr.length;i++)
        {
            asum+=arr[i];
        }
     return (int)(esum-asum);   
    }
}
